console.log("working");

var map = L.map("mapid", {
  center: [
    40.7, -94.5
  ],
  zoom: 3
});


  function getRadius(magnitude) {
    if (magnitude === 0) {
      return 1;
    }


    return div;
  };

  legend.addTo(map);
});
